import { createTheme } from "@mui/material/styles";
const theme = createTheme({
  palette: {
    primary: {
      main: "#FF9A00",
    },
    secondary: {
      main: "#fff",
    },
    error: {
      main: "#820000",
    },
    background: {
      default: "#021f35",
    },
  },
});

export default theme;
